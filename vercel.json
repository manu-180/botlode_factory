{
  "buildCommand": "flutter build web --release && mkdir -p docs && cp -r build/web/* docs/",
  "outputDirectory": "docs",
  "rewrites": [
    {
      "source": "/((?!sitemap\\.xml|robots\\.txt).*)",
      "destination": "/index.html"
    }
  ]
}
